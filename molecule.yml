---
dependency:
  name: galaxy
molecule:
  test:
    sequence:
      - destroy
      - create
      - converge
      - verify
  debug: true
ansible:
  playbook: ./tests/test.yml
driver:
  name: docker
docker:
  containers:
    - name: role-varnish-src
      image: ubuntu
      image_version: 16.04
      ansible_groups:
        - group1
verifier:
  name: testinfra